[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildContext = "."

[deploy]
startCommand = "/app/server"
restartPolicyType = "on_failure"

[env]
HOST = "0.0.0.0"
PORT = "8080"
WS_PORT = "8000"
LOG_LEVEL = "info"
WORKERS = "8"
MAX_CONNECTIONS = "50000"
